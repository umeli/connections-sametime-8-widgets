export interface Recording {
    placeId: String,
    size: number,
    meetingName: String,
    owner: String,
    fileId: String,
    sessionId: Boolean,
    createdAt: number,
    finishedAt: Number,
    updatedAt: Number,
    expireAt: Number,
    deleted: Boolean,
    isOwner: Boolean,
    sizeNize: String,
    createdNize: string

}
