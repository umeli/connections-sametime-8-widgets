export interface Recording {
    placeId: String,
    size: Number,
    meetingName: String,
    owner: String,
    fileId: String,
    sessionId: Boolean,
    createdAt: Number,
    finishedAt: Number,
    updatedAt: Number,
    expireAt: Number,
    deleted: Boolean,
    isOwner: Boolean
}
